将以下 JSON 数组中的日文字符串翻译成中文（简体）。
每个条目包含 id 和 t 两个字段，翻译 t 的内容，id 保持不变。
要求：
- 输出格式必须是 JSON 数组，每个元素格式为 {"id": <原id>, "t": "<中文翻译>"}
- 所有输入条目必须出现在输出中，id 不可省略或修改
- 只翻译文字内容，不要修改格式或标点之外的内容
- 游戏专有名词使用中文惯用译名
- 直接输出 JSON 数组，不要加任何说明或代码块标记

{texts}
